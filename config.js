window.MAUSAM_CONFIG = {
  // Optional OpenWeather API key. Keep secrets server-side for production.
  // The app uses Open-Meteo by default so the demo works without a key.
  OPENWEATHER_API_KEY: ""
};
